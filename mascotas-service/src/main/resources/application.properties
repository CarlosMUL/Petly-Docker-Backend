spring.application.name=mascotas-service
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/bd_Mascotas}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:220206Cj*}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=${JWT_SECRET}
jwt.expiration=3600000

spring.security.user.name=admin
spring.security.user.password=1234

OCI_USER_ID=${OCI_USER_ID:}
OCI_TENANCY_ID=${OCI_TENANCY_ID:}
OCI_FINGERPRINT=${OCI_FINGERPRINT:}
OCI_REGION=${OCI_REGION:sa-santiago-1}
OCI_NAMESPACE=${OCI_NAMESPACE:axgfwyf4tpjt}
OCI_BUCKET=${OCI_BUCKET:Mascotas-bucket}
OCI_PRIVATE_KEY_PATH=${OCI_PRIVATE_KEY_PATH:./oci_api_key.pem}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

server.port=${SERVER_PORT:8083}
